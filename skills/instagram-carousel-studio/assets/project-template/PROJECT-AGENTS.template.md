# {{PROJECT_NAME}}

## Carousel Contract

- Use the installed or bundled `instagram-carousel-studio` Skill for this project.
- Cards are exactly 1080×1350px; default to 10 unless the user explicitly requests 2–9.
- Lock topic and title before full production.
- Use one approved Style Capsule and editable HTML before final output.
- Never publish, send, deploy, schedule, or configure live automation without current-turn approval.

## Learning Contract

- Learning stays in this project.
- Record reviewed runs under `reviews/` using `KEEP / CHANGE / BAN / WHY`.
- Do not update a Style Capsule automatically.
- Three comparable independent uses may create a proposed update; Human approval is required before applying it.
- Do not write to external Vaults, shared memory, or the reusable Skill by default.
